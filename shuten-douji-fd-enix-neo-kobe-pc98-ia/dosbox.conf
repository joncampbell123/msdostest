# This is the configuration file for DOSBox 0.800. (Please use the latest version of DOSBox)
# Lines starting with a # are comment lines and are ignored by DOSBox.
# They are used to (briefly) document the effect of each option.
[dosbox]
memsize=8
machine=pc98
pc-98 sound bios=true
cascade interrupt ignore in service=true

[cpu]
core=normal
cputype=386
cycles=6000
# ^ Any faster and everybody looks as if they're running in place. Slow it down.

[autoexec]
imgmount 0 "Shuten Douji (Disk 1).hdm" -t floppy -fs none
imgmount 1 "Shuten Douji (Disk 2).hdm" -t floppy -fs none
boot -l a

