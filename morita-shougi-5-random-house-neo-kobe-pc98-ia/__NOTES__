Why the demo failed the test:
- Will eventually crash, if run from HDI image. Runs OK if mounted and run directly in DOSBox-X
- Strange mouse cursor confinement issues, check behavior on real hardware
